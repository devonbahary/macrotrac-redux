import React from 'react';

const MacrotracLogo = (props) => (
    <div className="MacrotracLogo">
        <span className="MacrotracLogo__logo ion-fork"></span>
    </div>
);

export default MacrotracLogo;
